set(PHYSICS_TARGET_NAME physics)

file(GLOB SOURCES CONFIGURE_DEPENDS *.cpp)
file(GLOB HEADERS CONFIGURE_DEPENDS *.h)

add_library(${PHYSICS_TARGET_NAME} STATIC
	${SOURCES}
	${HEADERS}
)

target_include_directories(${PHYSICS_TARGET_NAME}
	PUBLIC
		${CMAKE_CURRENT_LIST_DIR}
)
